# Copilot instructions

The instructions for this repository live in [`AGENTS.md`](../AGENTS.md). Read
that file and follow it.

This file exists only so that Copilot finds the instructions through its own
convention. It deliberately does not restate them: two copies drift apart, and
whichever copy is read first then wins. If something is missing, add it to
`AGENTS.md`.
